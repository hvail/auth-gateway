<!DOCTYPE html>
<html>

<head>
    <title>Authorization Result</title>
</head>

<body>
    <h1>Authorization Result</h1>
    <p>Thank you for authorizing the application.</p>

    <script>
        // const decodedStr = encodedStr.replace(/&#(\d+);/g, (match, dec) => {
        //     return String.fromCharCode(dec);
        // });

        var dataStr = "<%= data %>".replace(/&#(\d+);/g, (match, dec) => {
            return String.fromCharCode(dec);
        });
        console.log(dataStr); // This will log the data to the console
        var data = JSON.parse(dataStr); // Parse the JSON string into a JavaScript object
        console.log(data); // This will log the data to the console
        // Use the 'data' variable in your JavaScript code
    </script>
</body>

</html>